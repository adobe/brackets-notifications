{
  "name": "deployAppNotification",
  "version": "0.0.1",
  "description": "Upload the gzipped json files to the S3 bucket",
  "main": "deployAppNotification.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.0.18"
  }
}
